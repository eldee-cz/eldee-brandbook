{
  "name": "eldee-brandbook",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:hq": "PUBLIC_HQ_BUILD=1 astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-pdf": "npm run build:hq && node scripts/generate-pdf.mjs",
    "build:full": "npm run generate-pdf:oba && npm run build:hq",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "generate-pdf:verejny": "npm run build && node scripts/generate-pdf.mjs --verejny",
    "generate-pdf:oba": "npm run generate-pdf && npm run generate-pdf:verejny"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.7",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "@playwright/test": "^1.60.0",
    "opentype.js": "^2.0.0",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.60.0",
    "wawoff2": "^2.0.1"
  }
}
