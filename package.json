{
  "name": "eldee-brandbook",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-pdf": "npm run build && node scripts/generate-pdf.mjs",
    "build:full": "npm run build && node scripts/generate-pdf.mjs && npm run build",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.7",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "opentype.js": "^2.0.0",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.60.0",
    "wawoff2": "^2.0.1"
  }
}
